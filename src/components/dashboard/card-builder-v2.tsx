"use client";
/* eslint-disable @next/next/no-img-element */

import { useEffect, useMemo, useState } from "react";
import Link from "next/link";
import QRCode from "qrcode";
import { ArrowDown, ArrowLeft, ArrowUp, BarChart3, Check, ContactRound, Copy, Eye, ImagePlus, LayoutGrid, Link2, Loader2, Lock, Monitor, Palette, Plus, Save, Settings2, Share2, Smartphone, Sparkles, Trash2, Type, UploadCloud, X } from "lucide-react";
import { CardPreview } from "@/components/card/card-preview";
import type { CardData, CardWidgetType, ContactFormField, QuickAction, QuickActionType, SmartButton } from "@/lib/types";
import { cn } from "@/lib/utils";
import { plans } from "@/lib/config";
import type { PlanId } from "@/lib/types";
import type { AccessReason, FeatureKey } from "@/lib/plan-access";
import { downgradeImpact, featureLabels, lockMessages, paidPlanOrder, planFeatures, planLimits, planName, requiredPlanForFeature, requiredPlanForLimit } from "@/lib/plan-access";
import { TrialPlanPreview } from "@/components/dashboard/trial-plan-preview";
import { LockedOverlay } from "@/components/dashboard/locked-overlay";
import { AppChrome } from "@/components/app-chrome";
import { LockBadge, PlanLock, useUpgrade } from "@/components/upgrade-dialog";
import type { UpgradePrompt } from "@/components/upgrade-dialog";

type Tab = "structure" | "brand" | "appearance" | "actions" | "content" | "contact" | "leads" | "publish";
const tabs = [
  { id: "structure", label: "מבנה", icon: LayoutGrid }, { id: "brand", label: "מותג", icon: Palette }, { id: "appearance", label: "רקעים וצבעים", icon: Sparkles },
  { id: "actions", label: "פעולות", icon: Link2 }, { id: "content", label: "תוכן", icon: Type },
  { id: "contact", label: "שמירת איש קשר", icon: ContactRound }, { id: "leads", label: "לידים ומדידה", icon: BarChart3 }, { id: "publish", label: "פרסום", icon: Share2 },
] as const;
const widgetNames: Record<CardWidgetType, string> = { smart_buttons: "כפתורים חכמים", services: "שירותים", gallery: "גלריה / קרוסלה", video: "סרטון", testimonials: "המלצות", hours: "שעות פעילות", files: "קבצים", contact_form: "טופס צור קשר" };
const actionOptions: Array<{ value: QuickActionType; label: string; placeholder: string }> = [
  { value: "phone", label: "טלפון", placeholder: "050-0000000" }, { value: "whatsapp", label: "WhatsApp", placeholder: "972500000000" },
  { value: "email", label: "אימייל", placeholder: "name@business.co.il" }, { value: "website", label: "אתר", placeholder: "https://..." },
  { value: "waze", label: "Waze", placeholder: "כתובת העסק" }, { value: "google_maps", label: "Google Maps", placeholder: "כתובת העסק" },
  { value: "save_contact", label: "שמור אותי", placeholder: "נוצר אוטומטית מפרטי איש הקשר" },
  { value: "instagram", label: "Instagram", placeholder: "https://instagram.com/..." }, { value: "facebook", label: "Facebook", placeholder: "https://facebook.com/..." },
  { value: "linkedin", label: "LinkedIn", placeholder: "https://linkedin.com/in/..." }, { value: "tiktok", label: "TikTok", placeholder: "https://tiktok.com/@..." },
  { value: "youtube", label: "YouTube", placeholder: "https://youtube.com/@..." }, { value: "calendar", label: "קביעת פגישה", placeholder: "https://cal.com/..." },
];

export function CardBuilderV2({ initialCard, demo, siteUrl, planId, locked = false, lockReason = "active", trialActive = false, forceTour = false, initialPreviewMode = "mobile" }: { initialCard: CardData; demo: boolean; siteUrl: string; planId: PlanId; locked?: boolean; lockReason?: AccessReason; trialActive?: boolean; forceTour?: boolean; initialPreviewMode?: "mobile" | "desktop" }) {
  const [card, setCard] = useState(initialCard);
  const [tab, setTab] = useState<Tab>("structure");
  const [saving, setSaving] = useState(false);
  const [uploading, setUploading] = useState("");
  const [status, setStatus] = useState<{ type: "success" | "error"; text: string } | null>(null);
  const [qrData, setQrData] = useState("");
  const [previewMode, setPreviewMode] = useState<"mobile" | "desktop">(initialPreviewMode);
  const [showTour, setShowTour] = useState(forceTour);
  const [tourStep, setTourStep] = useState(0);
  const publicUrl = useMemo(() => `${siteUrl.replace(/\/$/, "")}/${card.slug}`, [siteUrl, card.slug]);
  const currentPlan = plans.find((plan) => plan.id === planId) || plans[0];
  const limits = planLimits(planId);
  const features = planFeatures(planId);
  const { requestUpgrade } = useUpgrade();

  // כל נעילה בממשק מצביעה על אותה נקודה: מה חסום, ואיזה מסלול פותח אותה.
  // איזה וידג'ט חסום במסלול הנוכחי, ובאיזו יכולת הוא תלוי.
  const widgetLock = (type: CardWidgetType): FeatureKey | null => {
    if (type === "video" && !features.video) return "video";
    if (type === "files" && !features.files) return "files";
    return null;
  };

  const lockPrompt = (feature: FeatureKey): UpgradePrompt => ({
    title: featureLabels[feature],
    description: `${featureLabels[feature]} אינו כלול במסלול ${currentPlan.name}. המסלול ${planName(requiredPlanForFeature(feature))} פותח את היכולת מיד לאחר הרכישה.`,
    requiredPlan: requiredPlanForFeature(feature),
  });
  const readiness = useMemo(() => {
    const checks = [
      { ok: Boolean(card.businessName.trim()), label: "שם העסק" },
      { ok: Boolean(card.ownerName.trim()), label: "שם מלא" },
      { ok: Boolean(card.slug.trim()), label: "כתובת הכרטיס" },
      { ok: card.quickActions.some((action) => action.value || action.type === "save_contact"), label: "פעולה מהירה אחת" },
      { ok: Boolean(card.seoTitle.trim()), label: "כותרת SEO" },
      { ok: Boolean(card.seoDescription.trim()), label: "תיאור SEO" },
      { ok: !card.coverUrl || Boolean(card.coverAlt.trim()), label: "טקסט חלופי לקאבר" },
      { ok: !card.logoUrl || Boolean(card.logoAlt.trim()), label: "טקסט חלופי ללוגו" },
    ];
    const completed = checks.filter((check) => check.ok).length;
    return { score: Math.round((completed / checks.length) * 100), missing: checks.find((check) => !check.ok)?.label };
  }, [card]);

  useEffect(() => {
    if (!demo) return;
    const frame = requestAnimationFrame(() => { const draft = localStorage.getItem("naimly-demo-card"); if (draft) try { setCard({ ...initialCard, ...(JSON.parse(draft) as Partial<CardData>) }); } catch { /* invalid local draft */ } });
    return () => cancelAnimationFrame(frame);
  }, [demo, initialCard]);
  useEffect(() => {
    if (!demo) return;
    const frame = requestAnimationFrame(() => { if (forceTour || !localStorage.getItem("naimly-demo-onboarding-seen")) setShowTour(true); });
    return () => cancelAnimationFrame(frame);
  }, [demo, forceTour]);
  useEffect(() => { QRCode.toDataURL(publicUrl, { width: 360, margin: 2, color: { dark: card.primaryColor, light: "#ffffff" } }).then(setQrData).catch(() => setQrData("")); }, [publicUrl, card.primaryColor]);
  function update<K extends keyof CardData>(key: K, value: CardData[K]) { setCard((current) => ({ ...current, [key]: value })); setStatus(null); }

  async function save() {
    if (locked) { setStatus({ type: "error", text: lockMessages[lockReason] }); return; }
    setSaving(true); setStatus(null);
    try {
      const response = await fetch("/api/cards", { method: "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(card) });
      const result = await response.json();
      if (!response.ok) {
        // 402/403 מהשרת = חסימת מסלול. פותחים ישירות את מסך הרכישה.
        if (result?.upgradeTo) {
          requestUpgrade({
            title: result.locked ? "המנוי אינו פעיל" : (result.error || "היכולת אינה כלולה במסלול"),
            description: result.error,
            requiredPlan: result.upgradeTo as PlanId,
          });
        }
        throw new Error(result.error || "השמירה נכשלה");
      }
      const next = { ...card, ...result.card } as CardData; setCard(next); if (demo) localStorage.setItem("naimly-demo-card", JSON.stringify(next));
      setStatus({ type: "success", text: card.isPublished ? "השינויים נשמרו והכרטיס פורסם" : "הטיוטה נשמרה" });
    } catch (error) { setStatus({ type: "error", text: error instanceof Error ? error.message : "לא הצלחנו לשמור" }); } finally { setSaving(false); }
  }
  async function upload(file: File, target: "coverUrl" | "logoUrl" | "avatarUrl" | "gallery") {
    if (target === "gallery" && card.gallery.length >= limits.galleryItems) {
      requestUpgrade({
        title: `הגעת ל‑${limits.galleryItems} תמונות בגלריה`,
        description: `מסלול ${currentPlan.name} כולל עד ${limits.galleryItems} תמונות. שדרוג פותח מכסה גדולה יותר, והתמונות הקיימות נשארות כפי שהן.`,
        requiredPlan: requiredPlanForLimit("galleryItems", limits.galleryItems + 1),
      });
      return;
    }
    setUploading(target); setStatus(null);
    try {
      let url = "";
      if (demo) { if (file.size > 2 * 1024 * 1024) throw new Error("בדמו ניתן להעלות תמונה עד 2MB"); url = await new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result)); reader.onerror = reject; reader.readAsDataURL(file); }); }
      else { const body = new FormData(); body.set("file", file); body.set("kind", "image"); const response = await fetch("/api/uploads", { method: "POST", body }); const result = await response.json(); if (!response.ok) throw new Error(result.error || "ההעלאה נכשלה"); url = result.url; }
      if (target === "gallery") update("gallery", [...card.gallery, url]); else update(target, url);
      setStatus({ type: "success", text: "התמונה הועלתה. יש לשמור כדי לפרסם." });
    } catch (error) { setStatus({ type: "error", text: error instanceof Error ? error.message : "ההעלאה נכשלה" }); } finally { setUploading(""); }
  }
  function moveWidget(index: number, direction: -1 | 1) { const next = [...card.widgets]; const target = index + direction; if (target < 0 || target >= next.length) return; [next[index], next[target]] = [next[target], next[index]]; update("widgets", next); }
  function addAction() { if (card.quickActions.length >= Math.min(card.quickActionsLimit, currentPlan.limits.quickActions)) return; update("quickActions", [...card.quickActions, { id: crypto.randomUUID(), type: "website", label: "קישור חדש", value: "" }]); }
  function patchAction(id: string, patch: Partial<QuickAction>) { update("quickActions", card.quickActions.map((item) => item.id === id ? { ...item, ...patch } : item)); }
  function changeActionType(item: QuickAction, type: QuickActionType) { const option = actionOptions.find((entry) => entry.value === type); const autoValue = type === "phone" ? card.phone : type === "whatsapp" ? card.whatsapp : type === "email" ? card.email : type === "website" ? card.website : ["waze", "google_maps"].includes(type) ? card.address : ""; patchAction(item.id, { type, label: option?.label || item.label, value: autoValue }); }
  function addButton() { const button: SmartButton = { id: crypto.randomUUID(), label: "כפתור חדש", description: "הסבר קצר על הפעולה", action: "url", value: "" }; update("smartButtons", [...card.smartButtons, button]); }
  function patchButton(id: string, patch: Partial<SmartButton>) { update("smartButtons", card.smartButtons.map((item) => item.id === id ? { ...item, ...patch } : item)); }
  function addFormField() { const field: ContactFormField = { id: crypto.randomUUID(), label: "שדה חדש", type: "text", required: false }; update("contactFormFields", [...card.contactFormFields, field]); }
  function patchFormField(id: string, patch: Partial<ContactFormField>) { update("contactFormFields", card.contactFormFields.map((item) => item.id === id ? { ...item, ...patch } : item)); }
  function closeTour() { localStorage.setItem("naimly-demo-onboarding-seen", "true"); setShowTour(false); }

  const formPreview = <div className="rounded-2xl bg-[#f6f7fb] p-4"><h3 className="font-extrabold">{card.contactFormTitle}</h3><div className="mt-3 grid gap-2">{card.contactFormFields.slice(0, 4).map((field) => <div key={field.id} className="min-h-10 rounded-xl border border-[#dfe4ec] bg-white px-3 py-2 text-xs text-[#7b8799]">{field.label}{field.required ? " *" : ""}</div>)}<div className="grid min-h-10 place-items-center rounded-xl bg-[var(--card-primary)] text-xs font-bold text-white">שליחת פנייה</div></div></div>;

  return <div className="mx-auto max-w-[1500px] pb-28 xl:pb-0">
    <AppChrome variant="builder" />
    <div className="flex flex-col gap-4 border-b border-[#dfe5ed] pb-5 sm:flex-row sm:items-end sm:justify-between">
      <div className="min-w-0">
        <div className="flex items-center gap-2 text-sm font-bold text-[#6d4aff]"><Sparkles size={16} />Smart Card Builder</div>
        <h1 className="mt-1 text-2xl font-black tracking-[-0.04em] sm:text-3xl">בונים מבלוקים. רואים תוצאה מיד.</h1>
        <p className="mt-1 text-sm text-[#718096]">מוסיפים רכיבים, משנים סדר ומפרסמים — בלי קוד.</p>
      </div>
      <div className="flex flex-wrap gap-2">
        {demo && <button type="button" onClick={() => { setTourStep(0); setShowTour(true); }} className="button-secondary min-h-11 flex-1 sm:flex-none"><Sparkles size={17} />סיור קצר</button>}
        {card.isPublished && <Link href={`/${card.slug}`} target="_blank" className="button-secondary min-h-11 flex-1 sm:flex-none"><Eye size={17} />צפייה בכרטיס</Link>}
        {/* בנייד כפתור השמירה חי בסרגל התחתון הצף, כדי שיהיה תמיד בהישג אצבע. */}
        <button type="button" onClick={save} disabled={saving || locked} className="button-primary hidden min-w-40 sm:inline-flex">{saving ? <Loader2 size={17} className="animate-spin" /> : <Save size={17} />}{saving ? "שומר..." : "שמירה ופרסום"}</button>
      </div>
    </div>
    {locked && <LockedOverlay reason={lockReason} />}
    {trialActive && <TrialPlanPreview impactByPlan={paidPlanOrder.map((plan) => ({ plan, name: planName(plan), items: downgradeImpact(card, plan) }))} />}
    {status && <div role={status.type === "error" ? "alert" : "status"} className={cn("mt-4 flex items-center justify-between rounded-xl border p-3 text-sm", status.type === "success" ? "border-[#b7e6d8] bg-[#effcf8] text-[#08735f]" : "border-[#f0bdc3] bg-[#fff2f4] text-[#a32031]")}><span className="flex items-center gap-2">{status.type === "success" ? <Check size={17} /> : <X size={17} />}{status.text}</span><button type="button" onClick={() => setStatus(null)} aria-label="סגירה"><X size={16} /></button></div>}
    <div className="mt-4 rounded-2xl border border-[#ded8ff] bg-[#f7f5ff] p-4" aria-label={`מוכנות לפרסום ${readiness.score} אחוז`}><div className="flex items-center justify-between gap-3 text-sm"><strong>בדיקת מוכנות לפרסום</strong><span className="font-black text-[#5134cc]">{readiness.score}%</span></div><div className="mt-2 h-2 overflow-hidden rounded-full bg-white" aria-hidden="true"><div className="h-full rounded-full bg-[linear-gradient(90deg,#6d4aff,#14d9c4)] transition-all" style={{ width: `${readiness.score}%` }} /></div><p className="mt-2 text-xs text-[#68758a]">{readiness.missing ? `השלב הבא המומלץ: ${readiness.missing}` : "מעולה — הכרטיס מוכן לפרסום."}</p></div>
    <div className={cn("mt-5 grid gap-5", previewMode === "mobile" ? "xl:grid-cols-[minmax(0,1fr)_390px]" : "xl:grid-cols-1")}>
      <section className="card-surface min-w-0 overflow-hidden"><div className="overflow-x-auto overscroll-x-contain border-b border-[#e2e6ee] px-3 pt-3 [scrollbar-width:none] [&::-webkit-scrollbar]:hidden"><div className="flex min-w-max gap-1" role="tablist" aria-label="שלבי בניית הכרטיס">{tabs.map(({ id, label, icon: Icon }) => <button key={id} type="button" role="tab" aria-selected={tab === id} onClick={() => setTab(id)} className={cn("flex min-h-12 items-center gap-2 rounded-t-xl border-b-2 px-3.5 text-sm font-bold", tab === id ? "border-[#6d4aff] bg-[#f5f2ff] text-[#4b3bad]" : "border-transparent text-[#748196] hover:bg-[#f6f7fa]")}><Icon size={17} />{label}</button>)}</div></div>
        <div className="p-4 sm:p-6 lg:p-7">
          {tab === "structure" && <Section title="הווידג׳טים בכרטיס" text="הפעילו רכיבים ושנו את סדר ההופעה שלהם."><div className="grid gap-2">{card.widgets.map((widget, index) => <div key={widget.id} className="flex items-center gap-3 rounded-2xl border border-[#e2e6ee] p-3"><span className="grid h-10 w-10 place-items-center rounded-xl bg-[#f1efff] text-[#6d4aff]"><LayoutGrid size={18} /></span><label className="min-w-0 flex-1"><span className="block text-sm font-bold">{widgetNames[widget.type]}</span><input className="mt-1 w-full bg-transparent text-xs text-[#758198] outline-none" value={widget.title} onChange={(e) => update("widgets", card.widgets.map((item) => item.id === widget.id ? { ...item, title: e.target.value } : item))} aria-label={`כותרת ${widgetNames[widget.type]}`} /></label><button type="button" onClick={() => moveWidget(index, -1)} disabled={index === 0} className="grid h-11 w-11 place-items-center rounded-lg hover:bg-[#f2f4f8] disabled:opacity-25" aria-label="העברה למעלה"><ArrowUp size={16} /></button><button type="button" onClick={() => moveWidget(index, 1)} disabled={index === card.widgets.length - 1} className="grid h-11 w-11 place-items-center rounded-lg hover:bg-[#f2f4f8] disabled:opacity-25" aria-label="העברה למטה"><ArrowDown size={16} /></button>{widgetLock(widget.type) ? <button type="button" onClick={() => requestUpgrade(lockPrompt(widgetLock(widget.type)!))} className="flex h-11 items-center gap-1 rounded-full border border-dashed border-[#d5cdf5] px-3 text-[11px] font-bold text-[#6d4aff]" aria-label={`${widgetNames[widget.type]} — נעול במסלול הנוכחי`}><Lock size={12} />{planName(requiredPlanForFeature(widgetLock(widget.type)!))}</button> : <button type="button" role="switch" aria-checked={widget.enabled} onClick={() => update("widgets", card.widgets.map((item) => item.id === widget.id ? { ...item, enabled: !item.enabled } : item))} className={cn("relative h-7 w-12 rounded-full transition", widget.enabled ? "bg-[#6d4aff]" : "bg-[#c7ced9]")}><span className={cn("absolute top-1 h-5 w-5 rounded-full bg-white transition", widget.enabled ? "right-6" : "right-1")} /></button>}</div>)}</div></Section>}
          {tab === "brand" && <div className="grid gap-8"><Section title="זהות העסק" text="שם, כותרת וסלוגן שמופיעים בראש הכרטיס."><div className="grid gap-4 sm:grid-cols-2"><Field label="שם העסק"><input className="field-input" value={card.businessName} onChange={(e) => update("businessName", e.target.value)} /></Field><Field label="שם מלא"><input className="field-input" value={card.ownerName} onChange={(e) => update("ownerName", e.target.value)} /></Field><Field label="כותרת מתחת ללוגו"><input className="field-input" value={card.roleTitle} onChange={(e) => update("roleTitle", e.target.value)} /></Field><Field label="סלוגן"><input className="field-input" value={card.slogan} onChange={(e) => update("slogan", e.target.value)} maxLength={140} /></Field><Field label="אודות" wide><textarea className="field-textarea" value={card.bio} onChange={(e) => update("bio", e.target.value)} /></Field></div></Section><Section title="תמונות ולוגו" text="קאבר רחב, לוגו או תמונת פרופיל."><div className="grid gap-3 sm:grid-cols-3"><UploadTile label="תמונת קאבר" busy={uploading === "coverUrl"} onFile={(file) => upload(file, "coverUrl")} /><UploadTile label="לוגו העסק" busy={uploading === "logoUrl"} onFile={(file) => upload(file, "logoUrl")} /><UploadTile label="תמונת פרופיל" busy={uploading === "avatarUrl"} onFile={(file) => upload(file, "avatarUrl")} /></div><div className="mt-4"><span className="field-label">צורת הלוגו</span><div className="mt-2 flex gap-2">{(["circle", "rounded", "square"] as const).map((shape) => <button key={shape} type="button" onClick={() => update("logoShape", shape)} className={cn("min-h-11 px-4 text-sm font-bold", shape === "circle" ? "rounded-full" : shape === "rounded" ? "rounded-xl" : "rounded-none", card.logoShape === shape ? "bg-[#6d4aff] text-white" : "border border-[#dfe4ec] bg-white")}>{shape === "circle" ? "עגול" : shape === "rounded" ? "מעוגל" : "מרובע"}</button>)}</div></div></Section><Section title="צבע ותבנית" text="המיתוג מתעדכן בכל הרכיבים."><div className="grid gap-4 sm:grid-cols-2"><Field label="צבע ראשי"><input type="color" className="h-12 w-full rounded-xl border border-[#dfe4ec] p-1" value={card.primaryColor} onChange={(e) => update("primaryColor", e.target.value)} /></Field><Field label="צבע משלים"><input type="color" className="h-12 w-full rounded-xl border border-[#dfe4ec] p-1" value={card.accentColor} onChange={(e) => update("accentColor", e.target.value)} /></Field></div></Section></div>}
          {tab === "actions" && <div className="grid gap-8"><Section title="אייקונים לפעולות מהירות" text={`המערכת בונה את הקישור הנכון לפי סוג הפעולה. במסלול שלך זמינים עד ${currentPlan.limits.quickActions}.`}><div className="mb-4 flex gap-2">{([3, 6, 9] as const).map((limit) => { const locked = limit > limits.quickActions; return <button key={limit} type="button" className={cn("flex min-h-10 items-center gap-1.5 rounded-xl border px-4 text-sm font-bold", locked ? "border-dashed border-[#d5cdf5] text-[#8b7fd4]" : card.quickActionsLimit === limit ? "border-[#6d4aff] bg-[#f1efff] text-[#4b3bad]" : "border-[#dfe4ec]")} onClick={() => locked ? requestUpgrade({ title: `${limit} פעולות מהירות`, description: `מסלול ${currentPlan.name} כולל עד ${limits.quickActions} פעולות מהירות.`, requiredPlan: requiredPlanForLimit("quickActions", limit) }) : update("quickActionsLimit", limit)}>{locked && <Lock size={13} />}{limit} אייקונים</button>; })}</div><div className="grid gap-3">{card.quickActions.slice(0, card.quickActionsLimit).map((item) => <div key={item.id} className="grid gap-2 rounded-2xl border border-[#e2e6ee] p-3 sm:grid-cols-[155px_1fr_1fr_42px]"><select className="field-select" value={item.type} onChange={(e) => changeActionType(item, e.target.value as QuickActionType)} aria-label="סוג פעולה">{actionOptions.map((option) => <option key={option.value} value={option.value}>{option.label}</option>)}</select><input className="field-input" value={item.label} onChange={(e) => patchAction(item.id, { label: e.target.value })} aria-label="שם האייקון" /><input className="field-input" dir="ltr" value={item.value} onChange={(e) => patchAction(item.id, { value: e.target.value })} placeholder={actionOptions.find((entry) => entry.value === item.type)?.placeholder} aria-label="ערך הפעולה" /><button type="button" className="grid h-11 w-11 place-items-center rounded-xl text-[#a73342] hover:bg-[#fff0f2]" onClick={() => update("quickActions", card.quickActions.filter((action) => action.id !== item.id))} aria-label="מחיקה"><Trash2 size={17} /></button></div>)}</div><button type="button" className="button-secondary mt-3" disabled={card.quickActions.length >= Math.min(card.quickActionsLimit, currentPlan.limits.quickActions)} onClick={addAction}><Plus size={17} />הוספת אייקון</button></Section><Section title="כפתורים חכמים" text="קישור, תפריט, קביעת פגישה, תמונה או פעולה ישירה."><div className="grid gap-3">{card.smartButtons.map((button) => <div key={button.id} className="grid gap-3 rounded-2xl border border-[#e2e6ee] p-4"><div className="grid gap-3 sm:grid-cols-[1fr_180px_42px]"><input className="field-input" value={button.label} onChange={(e) => patchButton(button.id, { label: e.target.value })} aria-label="כותרת כפתור" /><select className="field-select" value={button.action} onChange={(e) => patchButton(button.id, { action: e.target.value as SmartButton["action"] })} aria-label="פעולת כפתור"><option value="url">קישור</option><option value="menu">תפריט / קטלוג</option><option value="booking">קביעת פגישה</option><option value="image">פתיחת תמונה</option><option value="phone">טלפון</option><option value="whatsapp">WhatsApp</option><option value="email">אימייל</option><option value="waze">Waze</option><option value="google_maps">Google Maps</option></select><button type="button" className="grid h-11 w-11 place-items-center rounded-xl text-[#a73342]" onClick={() => update("smartButtons", card.smartButtons.filter((item) => item.id !== button.id))} aria-label="מחיקה"><Trash2 size={17} /></button></div><input className="field-input" value={button.description} onChange={(e) => patchButton(button.id, { description: e.target.value })} placeholder="תיאור קצר" /><input className="field-input" dir="ltr" value={button.value} onChange={(e) => patchButton(button.id, { value: e.target.value })} placeholder="קישור, כתובת או מספר" /></div>)}</div><button type="button" className="button-secondary mt-3" onClick={addButton}><Plus size={17} />הוספת כפתור</button></Section></div>}
          {tab === "content" && <div className="grid gap-8"><Section title="פרטי קשר" text="משמשים גם ליצירת הפעולות החכמות."><div className="grid gap-4 sm:grid-cols-2"><Field label="טלפון"><input className="field-input" dir="ltr" value={card.phone} onChange={(e) => update("phone", e.target.value)} /></Field><Field label="WhatsApp"><input className="field-input" dir="ltr" value={card.whatsapp} onChange={(e) => update("whatsapp", e.target.value)} /></Field><Field label="אימייל"><input className="field-input" dir="ltr" value={card.email} onChange={(e) => update("email", e.target.value)} /></Field><Field label="אתר"><input className="field-input" dir="ltr" value={card.website} onChange={(e) => update("website", e.target.value)} /></Field><Field label="כתובת — ממנה נוצרים Waze ו־Maps" wide><input className="field-input" value={card.address} onChange={(e) => update("address", e.target.value)} /></Field></div></Section><Section title="גלריה וקרוסלה" text={`מעלים תמונות ובוחרים כיצד להציג אותן. מסלול ${currentPlan.name} כולל עד ${limits.galleryItems} תמונות (${card.gallery.length} בשימוש).`} badge={!features.carousel ? <LockBadge prompt={lockPrompt("carousel")} /> : null}><div className="flex gap-2">{(["grid", "carousel"] as const).map((style) => { const locked = style === "carousel" && !features.carousel; return <button key={style} type="button" onClick={() => locked ? requestUpgrade(lockPrompt("carousel")) : update("galleryStyle", style)} className={cn("min-h-10 rounded-xl border px-4 text-sm font-bold", locked ? "border-dashed border-[#d5cdf5] text-[#8b7fd4]" : card.galleryStyle === style ? "border-[#6d4aff] bg-[#f1efff] text-[#4b3bad]" : "border-[#dfe4ec]")}>{locked && <Lock size={13} className="ms-1 inline" />}{style === "grid" ? "גלריה" : "קרוסלה"}</button>; })}</div><UploadTile label="הוספת תמונה" busy={uploading === "gallery"} onFile={(file) => upload(file, "gallery")} /><div className="mt-3 grid grid-cols-3 gap-2 sm:grid-cols-5">{card.gallery.map((url, index) => <div key={`${url}-${index}`} className="relative"><img src={url} alt="" className="aspect-square w-full rounded-xl object-cover" /><button type="button" className="absolute left-1 top-1 grid h-7 w-7 place-items-center rounded-full bg-white text-[#a73342] shadow" onClick={() => update("gallery", card.gallery.filter((_, i) => i !== index))} aria-label="הסרת תמונה"><X size={14} /></button></div>)}</div></Section><Section title="סרטון YouTube" text="הסרטון מוטמע בכרטיס ללא יציאה מהעמוד."><input className="field-input" dir="ltr" value={card.videoUrl} onChange={(e) => update("videoUrl", e.target.value)} placeholder="https://youtube.com/watch?v=..." /></Section></div>}
          {tab === "contact" && <Section title="כפתור שמירה באנשי קשר" text="הפרטים הבאים נארזים בקובץ vCard. כשהמבקר לוחץ על ‘שמור אותי’, הטלפון מציע להוסיף אותם לאנשי הקשר."><div className="grid gap-4 sm:grid-cols-2"><Field label="שם מלא"><input className="field-input" value={card.vcard.fullName} onChange={(e) => update("vcard", { ...card.vcard, fullName: e.target.value })} /></Field><Field label="שם העסק"><input className="field-input" value={card.vcard.organization} onChange={(e) => update("vcard", { ...card.vcard, organization: e.target.value })} /></Field><Field label="תפקיד"><input className="field-input" value={card.vcard.title} onChange={(e) => update("vcard", { ...card.vcard, title: e.target.value })} /></Field><Field label="טלפון"><input className="field-input" dir="ltr" value={card.vcard.phone} onChange={(e) => update("vcard", { ...card.vcard, phone: e.target.value })} /></Field><Field label="אימייל"><input className="field-input" dir="ltr" value={card.vcard.email} onChange={(e) => update("vcard", { ...card.vcard, email: e.target.value })} /></Field><Field label="אתר"><input className="field-input" dir="ltr" value={card.vcard.website} onChange={(e) => update("vcard", { ...card.vcard, website: e.target.value })} /></Field><Field label="כתובת" wide><input className="field-input" value={card.vcard.address} onChange={(e) => update("vcard", { ...card.vcard, address: e.target.value })} /></Field><Field label="הערה שתישמר" wide><textarea className="field-textarea" value={card.vcard.note} onChange={(e) => update("vcard", { ...card.vcard, note: e.target.value })} /></Field></div><button type="button" className="button-secondary mt-4" onClick={() => { const exists = card.quickActions.some((item) => item.type === "save_contact"); if (!exists && card.quickActions.length < currentPlan.limits.quickActions) update("quickActions", [...card.quickActions, { id: crypto.randomUUID(), type: "save_contact", label: "שמור אותי", value: "" }]); setTab("actions"); }}><Plus size={17} />הוספת הכפתור לפעולות</button></Section>}
          {tab === "leads" && <div className="grid gap-8"><Section title="טופס צור קשר דינמי" text="בוחרים שדות, חובה או רשות, והסדר נשמר בכרטיס."><div className="grid gap-4 sm:grid-cols-2"><Field label="כותרת הטופס"><input className="field-input" value={card.contactFormTitle} onChange={(e) => update("contactFormTitle", e.target.value)} /></Field><Field label="הודעה לאחר שליחה"><input className="field-input" value={card.contactFormSuccessMessage} onChange={(e) => update("contactFormSuccessMessage", e.target.value)} /></Field></div><div className="mt-4 grid gap-2">{card.contactFormFields.map((field) => <div key={field.id} className="grid gap-2 rounded-xl border border-[#e2e6ee] p-3 sm:grid-cols-[1fr_150px_auto_42px]"><input className="field-input" value={field.label} onChange={(e) => patchFormField(field.id, { label: e.target.value })} aria-label="שם השדה" /><select className="field-select" value={field.type} onChange={(e) => patchFormField(field.id, { type: e.target.value as ContactFormField["type"] })} aria-label="סוג שדה"><option value="text">טקסט</option><option value="tel">טלפון</option><option value="email">אימייל</option><option value="textarea">טקסט ארוך</option><option value="select">בחירה</option><option value="checkbox">סימון</option></select><label className="flex min-h-11 items-center gap-2 text-sm"><input type="checkbox" checked={field.required} onChange={(e) => patchFormField(field.id, { required: e.target.checked })} />חובה</label><button type="button" className="grid h-11 w-11 place-items-center rounded-xl text-[#a73342]" onClick={() => update("contactFormFields", card.contactFormFields.filter((item) => item.id !== field.id))} aria-label="מחיקה"><Trash2 size={17} /></button></div>)}</div><button type="button" className="button-secondary mt-3" onClick={addFormField}><Plus size={17} />הוספת שדה</button></Section><Section title="מדידה ופרסום" text="הקודים נטענים רק לאחר הסכמת המבקר לעוגיות." badge={!features.tracking ? <LockBadge prompt={lockPrompt("tracking")} /> : null}><PlanLock locked={!features.tracking} prompt={lockPrompt("tracking")}><div className="grid gap-4 sm:grid-cols-3"><Field label="Google Analytics"><input className="field-input" dir="ltr" value={card.tracking.googleAnalyticsId} onChange={(e) => update("tracking", { ...card.tracking, googleAnalyticsId: e.target.value.toUpperCase() })} placeholder="G-XXXXXXXX" /></Field><Field label="Google Tag Manager"><input className="field-input" dir="ltr" value={card.tracking.googleTagManagerId} onChange={(e) => update("tracking", { ...card.tracking, googleTagManagerId: e.target.value.toUpperCase() })} placeholder="GTM-XXXXXXX" /></Field><Field label="Meta Pixel"><input className="field-input" dir="ltr" value={card.tracking.metaPixelId} onChange={(e) => update("tracking", { ...card.tracking, metaPixelId: e.target.value })} placeholder="1234567890" /></Field></div></PlanLock></Section></div>}
          {tab === "publish" && <div className="grid gap-8"><Section title="כתובת הכרטיס" text="הקישור וה־QR נשארים קבועים גם כשהתוכן משתנה."><div className="grid gap-4 sm:grid-cols-[1fr_auto]"><Field label="כתובת אישית" required><div className="flex" dir="ltr"><span className="grid place-items-center rounded-l-xl border border-r-0 border-[#dfe4ec] bg-[#f5f7fa] px-3 text-xs text-[#667389]">/{card.slug}</span><input className="field-input rounded-l-none" value={card.slug} required minLength={3} onChange={(e) => update("slug", e.target.value.toLowerCase().replace(/[^a-z0-9-]/g, ""))} /></div></Field><label className="flex min-h-12 items-center gap-2 self-end rounded-xl border border-[#dfe4ec] px-4 text-sm font-bold"><input type="checkbox" checked={card.isPublished} onChange={(e) => update("isPublished", e.target.checked)} />כרטיס פעיל</label></div><div className="mt-4 flex flex-wrap gap-2"><button type="button" className="button-secondary" onClick={() => { void navigator.clipboard.writeText(publicUrl); setStatus({ type: "success", text: "הקישור הועתק" }); }}><Copy size={17} />העתקת קישור</button>{qrData && <a className="button-secondary" href={qrData} download={`${card.slug}-qr.png`}><UploadCloud size={17} />הורדת QR</a>}</div></Section><Section title="נראות במנועי חיפוש" text="שליטה באינדוקס, בתצוגת Google ובשיתוף ברשתות." badge={!features.seo ? <LockBadge prompt={lockPrompt("seo")} /> : null}><div className="grid gap-4"><label className="flex min-h-12 items-center gap-3 rounded-xl border border-[#dfe4ec] px-4 text-sm font-bold"><input type="checkbox" checked={card.allowIndexing} onChange={(e) => update("allowIndexing", e.target.checked)} />לאפשר הצגה במנועי חיפוש</label><Field label="כותרת SEO"><input className="field-input" value={card.seoTitle} onChange={(e) => update("seoTitle", e.target.value)} maxLength={70} /></Field><Field label="תיאור SEO"><textarea className="field-textarea" value={card.seoDescription} onChange={(e) => update("seoDescription", e.target.value)} maxLength={170} /></Field><PlanLock locked={!features.seo} prompt={lockPrompt("seo")}><div className="grid gap-4 sm:grid-cols-2"><Field label="אזור שירות"><input className="field-input" value={card.areaServed} onChange={(e) => update("areaServed", e.target.value)} placeholder="לדוגמה: תל אביב והמרכז" /></Field><Field label="תמונת שיתוף 1200×630"><input className="field-input" type="url" dir="ltr" value={card.socialImageUrl} onChange={(e) => update("socialImageUrl", e.target.value)} placeholder="https://..." /></Field></div></PlanLock><div className="rounded-2xl border border-[#dfe4ec] bg-white p-4"><span className="text-xs text-[#16825d]">{new URL(publicUrl).hostname} › {card.slug}</span><strong className="mt-1 block text-lg text-[#283f8f]">{card.seoTitle || card.businessName}</strong><p className="mt-1 text-sm leading-6 text-[#56647a]">{card.seoDescription || "תיאור הכרטיס יופיע כאן בתוצאות החיפוש."}</p></div><div className="grid gap-4 sm:grid-cols-3"><Field label="תיאור תמונת קאבר"><input className="field-input" value={card.coverAlt} onChange={(e) => update("coverAlt", e.target.value)} /></Field><Field label="תיאור הלוגו"><input className="field-input" value={card.logoAlt} onChange={(e) => update("logoAlt", e.target.value)} /></Field><Field label="תיאור תמונת הפרופיל"><input className="field-input" value={card.avatarAlt} onChange={(e) => update("avatarAlt", e.target.value)} /></Field></div></div></Section></div>}
          {tab === "appearance" && <AppearancePanel card={card} onPatch={(patch) => { setCard((current) => ({ ...current, ...patch })); setStatus(null); }} />}
        </div>
      </section>
      <aside id="live-preview" className={cn("scroll-mt-4", previewMode === "mobile" && "xl:sticky xl:top-4 xl:self-start")}><div className="mb-3 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between"><div><span className="text-sm font-bold">תצוגה חיה</span><p className="mt-0.5 text-xs text-[#748196]">כך הכרטיס ייראה אצל המבקרים</p></div><div className="inline-flex self-start rounded-xl border border-[#dfe4ec] bg-white p-1" role="group" aria-label="בחירת גודל תצוגה"><button type="button" onClick={() => setPreviewMode("mobile")} aria-pressed={previewMode === "mobile"} className={cn("flex min-h-10 items-center gap-2 rounded-lg px-3 text-sm font-bold", previewMode === "mobile" ? "bg-[#6d4aff] text-white" : "text-[#637086]")}><Smartphone size={17} />נייד</button><button type="button" onClick={() => setPreviewMode("desktop")} aria-pressed={previewMode === "desktop"} className={cn("flex min-h-10 items-center gap-2 rounded-lg px-3 text-sm font-bold", previewMode === "desktop" ? "bg-[#6d4aff] text-white" : "text-[#637086]")}><Monitor size={17} />מחשב</button></div></div>{previewMode === "mobile" ? <div className="mx-auto w-full max-w-[360px] overflow-y-auto rounded-[28px] border-4 border-[#172033] bg-white shadow-[0_22px_65px_rgba(11,24,48,.2)] sm:rounded-[34px] sm:border-[9px] xl:max-h-[770px]"><CardPreview card={card} contactForm={formPreview} /></div> : <div className="mx-auto w-full max-w-[1040px] overflow-hidden rounded-[22px] border border-[#cad2df] bg-[#eef1f6] shadow-[0_22px_65px_rgba(11,24,48,.16)]"><div className="flex h-11 items-center gap-2 border-b border-[#d8dee8] bg-white px-4" aria-hidden="true"><span className="h-3 w-3 rounded-full bg-[#ff6b61]" /><span className="h-3 w-3 rounded-full bg-[#f5bf4f]" /><span className="h-3 w-3 rounded-full bg-[#14d9c4]" /><span className="mx-auto rounded-lg bg-[#f2f4f8] px-20 py-1 text-[11px] text-[#758198]">{publicUrl}</span></div><div className="max-h-[760px] overflow-y-auto bg-[radial-gradient(circle_at_15%_10%,rgba(109,74,255,.13),transparent_32%),#eef1f6] p-8"><div className="mx-auto max-w-[620px] overflow-hidden rounded-[30px] border border-white bg-white shadow-[0_24px_70px_rgba(11,16,32,.15)]"><CardPreview card={card} contactForm={formPreview} /></div></div></div>}</aside>
    </div>
    <div className="fixed inset-x-0 bottom-0 z-40 flex items-center gap-2 border-t border-[#dfe4ec] bg-white/95 p-3 pb-[calc(.75rem+env(safe-area-inset-bottom))] shadow-[0_-12px_30px_rgba(11,16,32,.12)] backdrop-blur xl:hidden"><a href="#live-preview" className="button-secondary flex-1"><Eye size={17} />תצוגה</a><button type="button" onClick={save} disabled={saving} className="button-primary flex-[1.35]">{saving ? <Loader2 size={17} className="animate-spin" /> : <Save size={17} />}{saving ? "שומר..." : "שמירה ופרסום"}</button></div>
    {/* סרגל שמירה קבוע בתחתית המסך בנייד. מעל ניווט התחתית של המעטפת. */}
    <div className="fixed inset-x-0 bottom-[calc(60px+env(safe-area-inset-bottom))] z-40 border-t border-[#dfe5ed] bg-white/95 p-3 backdrop-blur-lg sm:hidden">
      <div className="flex items-center gap-2">
        <a href="#live-preview" className="button-secondary min-h-12 shrink-0 px-3" aria-label="מעבר לתצוגה חיה"><Eye size={17} /></a>
        <button type="button" onClick={save} disabled={saving || locked} className="button-primary min-h-12 flex-1">
          {saving ? <Loader2 size={17} className="animate-spin" /> : <Save size={17} />}{saving ? "שומר..." : locked ? "נעול — נדרש מסלול" : "שמירה ופרסום"}
        </button>
      </div>
    </div>
    {showTour && <DemoTour step={tourStep} onSkip={closeTour} onNext={() => { if (tourStep === 2) closeTour(); else setTourStep((step) => step + 1); }} />}
  </div>;
}

const tourSlides = [
  { icon: <LayoutGrid size={25} />, title: "ברוכים הבאים לבונה של NAIMLY", text: "הכרטיס בנוי מווידג׳טים. אפשר להפעיל רכיבים, לשנות סדר ולערוך כל פרט בלי קוד." },
  { icon: <Palette size={25} />, title: "מעצבים ורואים מיד", text: "בחרו צבעים, רקע, לוגו וקאבר. התצוגה החיה מתעדכנת בזמן אמת ואפשר לעבור בין נייד למחשב." },
  { icon: <Share2 size={25} />, title: "מפרסמים ומשתפים", text: "בסיום שומרים, מפעילים את הכרטיס ומשתפים קישור או QR. מד המוכנות יציג מה כדאי להשלים לפני הפרסום." },
];

function DemoTour({ step, onSkip, onNext }: { step: number; onSkip: () => void; onNext: () => void }) {
  const slide = tourSlides[step];
  return <div className="fixed inset-0 z-[140] grid place-items-center bg-[#070b16]/65 p-4 backdrop-blur-sm" role="presentation"><section role="dialog" aria-modal="true" aria-labelledby="demo-tour-title" className="max-h-[90vh] w-full max-w-lg overflow-y-auto overscroll-contain rounded-[30px] bg-white shadow-[0_30px_100px_rgba(7,11,22,.38)]"><div className="bg-[radial-gradient(circle_at_15%_15%,rgba(20,217,196,.28),transparent_34%),linear-gradient(135deg,#0b1020,#33236f)] p-7 text-white"><div className="flex items-start justify-between"><span className="grid h-13 w-13 place-items-center rounded-2xl bg-white/10 text-[#8ef3e4]">{slide.icon}</span><button type="button" onClick={onSkip} className="min-h-11 rounded-xl px-3 text-sm font-bold text-white/75 hover:bg-white/10" aria-label="דלג על ההסבר">דלג</button></div><p className="mt-7 text-sm font-bold text-[#8ef3e4]">היכרות קצרה · {step + 1} מתוך {tourSlides.length}</p><h2 id="demo-tour-title" className="mt-2 text-3xl font-black tracking-[-0.04em]">{slide.title}</h2><p className="mt-3 max-w-md leading-7 text-[#c8d0df]">{slide.text}</p></div><div className="flex items-center justify-between gap-4 p-5"><div className="flex gap-2" aria-label={`שלב ${step + 1} מתוך ${tourSlides.length}`}>{tourSlides.map((_, index) => <span key={index} className={cn("h-2 rounded-full transition-all", index === step ? "w-7 bg-[#6d4aff]" : "w-2 bg-[#d8dde6]")} />)}</div><button type="button" onClick={onNext} className="button-primary min-w-32">{step === tourSlides.length - 1 ? "מתחילים" : "הבא"}<ArrowLeft size={17} /></button></div></section></div>;
}

function Section({ title, text, badge, children }: { title: string; text: string; badge?: React.ReactNode; children: React.ReactNode }) { return <section><div className="mb-4 flex items-start gap-3"><span className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-[#f1efff] text-[#6d4aff]"><Settings2 size={17} /></span><div><h2 className="flex flex-wrap items-center gap-2 font-extrabold">{title}{badge}</h2><p className="mt-0.5 text-sm text-[#748196]">{text}</p></div></div>{children}</section>; }
function Field({ label, wide, required, children }: { label: string; wide?: boolean; required?: boolean; children: React.ReactNode }) { return <label className={cn("field-label", wide && "sm:col-span-2")}><span>{label}{required && <span className="required-field">חובה</span>}</span>{children}</label>; }
function UploadTile({ label, busy, onFile }: { label: string; busy: boolean; onFile: (file: File) => void }) { return <label className="mt-3 grid min-h-28 cursor-pointer place-items-center rounded-2xl border border-dashed border-[#bdc7d6] bg-[#f8f9fc] text-center"><span><ImagePlus size={22} className="mx-auto text-[#6d4aff]" /><strong className="mt-1 block text-sm">{busy ? "מעלה..." : label}</strong><small className="text-[#7c8799]">JPG, PNG או WebP</small></span><input type="file" className="sr-only" accept="image/png,image/jpeg,image/webp" disabled={busy} onChange={(e) => { const file = e.target.files?.[0]; if (file) onFile(file); e.target.value = ""; }} /></label>; }

const backgroundPresets: Array<{ id: CardData["backgroundPreset"]; label: string; preview: string }> = [
  { id: "aurora", label: "רך ובהיר", preview: "linear-gradient(135deg,#f4f1ff,#eef9ff)" },
  { id: "paper", label: "נייר נקי", preview: "linear-gradient(135deg,#fff,#f3efe7)" },
  { id: "sunset", label: "שקיעה", preview: "linear-gradient(135deg,#ffdfd1,#f2d7ff)" },
  { id: "ocean", label: "אוקיינוס", preview: "linear-gradient(135deg,#071a31,#0f7b8d)" },
  { id: "midnight", label: "Midnight", preview: "linear-gradient(135deg,#111827,#392b73)" },
  { id: "minimal", label: "Mesh צבעוני", preview: "radial-gradient(circle at 20% 20%,#ffd8f4,transparent 40%),radial-gradient(circle at 80% 30%,#c8ecff,transparent 42%),#eeeaff" },
];

function AppearancePanel({ card, onPatch }: { card: CardData; onPatch: (patch: Partial<CardData>) => void }) {
  const colors: Array<{ key: "primaryColor" | "accentColor" | "buttonColor" | "headingColor" | "bodyTextColor"; label: string }> = [
    { key: "primaryColor", label: "צבע ראשי" }, { key: "accentColor", label: "צבע משלים" }, { key: "buttonColor", label: "צבע כפתורים" }, { key: "headingColor", label: "צבע כותרות" }, { key: "bodyTextColor", label: "צבע טקסט" },
  ];
  return <div className="grid gap-8"><Section title="רקע הכרטיס" text="בחרו אווירה מוכנה. כל רקע נשמר עם הכרטיס הציבורי."><div className="grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{backgroundPresets.map((preset) => <button key={preset.id} type="button" onClick={() => onPatch({ backgroundPreset: preset.id })} className={cn("overflow-hidden rounded-2xl border-2 text-right transition", card.backgroundPreset === preset.id ? "border-[#6d4aff] shadow-[0_0_0_3px_#eeeaff]" : "border-[#dfe4ec] hover:border-[#a99feb]")}><span className="block h-24" style={{ background: preset.preview }} /><span className="block bg-white px-3 py-2 text-sm font-bold">{preset.label}</span></button>)}</div></Section><Section title="צבעי מותג מדויקים" text="ניתן לשלוט בנפרד בכפתורים, בכותרות ובטקסט."><div className="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">{colors.map(({ key, label }) => <Field key={key} label={label}><div className="flex items-center gap-3 rounded-xl border border-[#dfe4ec] bg-white p-2"><input type="color" value={card[key]} onChange={(event) => onPatch({ [key]: event.target.value })} className="h-11 w-14 cursor-pointer rounded-lg border-0 bg-transparent p-0" /><input dir="ltr" value={card[key]} onChange={(event) => onPatch({ [key]: event.target.value })} className="min-w-0 flex-1 bg-transparent text-sm font-bold uppercase outline-none" maxLength={7} aria-label={label} /></div></Field>)}</div></Section></div>;
}
