# מצב הרגרסיה — מה בוצע ומה נשאר

**עדכון אחרון:** 2026-09-16 · ענף `main` · קומיט אחרון `860e560`

מסמך מעקב מול הפרומפט שנמסר ב-16.09.2026 ("תיקון ממצאי רגרסיה").
נכתב כדי שאפשר יהיה להמשיך מכאן בלי להסתמך על זיכרון השיחה.

> **הערה על החומרים:** שני קובצי ה-xlsx שהוזכרו בפרומפט —
> `NAIMLY_QA_Report_2026-09-12.xlsx` ו-`NAIMLY_QA_Regression_2026-09-15.xlsx` —
> **אינם קיימים בריפו**. העבודה בוצעה מהממצאים המפורטים בגוף הפרומפט,
> שהיו מלאים דיים. אם הקבצים יועלו, יש להצליב מולם לפני סגירת הרגרסיה.

---

## תמונת מצב

| קבוצה | ממצאים | סטטוס |
| --- | --- | --- |
| **1 — תקלות חוסמות** | QA-008, QA-013, QA-032, NEW-006, NEW-007 | ✅ **הושלם** · `0f2366f` |
| **2 — טפסים והתראות** | QA-006, QA-009, QA-033, NEW-005, REQ-016 | ✅ **הושלם** · `860e560` |
| **3 — סנכרון הכרטיס** | QA-024, QA-025, QA-026, QA-027, NEW-004 | ⬜ **טרם החל** |
| **4 — שפה, SEO ותוכן** | QA-035, NEW-001, NEW-002, NEW-003 | ⬜ **טרם החל** — ראו התנגשות למטה |
| **5 — בדיקות חסומות** | QA-014 עד QA-034 (17 ממצאים) | ⬜ **טרם החל** |

**חליפות נכון לעכשיו:** 446 יחידה · 110 E2E · 17 RLS · 55 אבטחה · Lighthouse 12/12.
**מיגרציות:** עד `027_lead_delivery_status.sql` — כולן הורצו ואומתו מול המסד החי.

---

## ⚠️ התנגשות שדורשת את הכרעתך

**קבוצה 4 כוללת את QA-035 (בורר שפה מלא באתר), אבל ב-15.09.2026 החלטת
להעביר את REQ-025 ל-HOLD.**

- QA-035 בפרומפט הנוכחי דורש: בורר שפה בדף הבית, במחירון, ביצירת קשר,
  בהרשמה ובדשבורד, ותרגום של הניווט, הטפסים, הודעות השגיאה, ה-Cookies,
  המחירון, העורך, המיילים וה-SEO.
- ההחלטה מ-15.09 (מתועדת ב-`DECISIONS.md`) קבעה: **שפת הכרטיס הציבורי
  בלבד**. זה בוצע ופעיל.

**שתי האפשרויות:**
1. QA-035 נשאר ב-HOLD — קבוצה 4 מצטמצמת ל-NEW-001, NEW-002, NEW-003.
2. ה-HOLD מבוטל — נדרש מנגנון i18n מרכזי, העדפת שפה ברמת המשתמש,
   `dir`/`lang` ברמת המסמך ותרגום המיילים. זו העבודה הגדולה ביותר שנותרה.

**לא אתקדם בקבוצה 4 עד שתכריע.**

---

## מה בוצע — קבוצה 1

### QA-008 / QA-013 · טופס הלידים · נסגר

**הממצא אומת במלואו:** אפס שדות בשלושת הכרטיסים הציבוריים.

**סיבת שורש:** `arrayValue` נפל לברירת מחדל רק כאשר הערך **אינו מערך**,
וברירת המחדל בעמודה `contact_form_fields` היא `'[]'::jsonb`. **מערך ריק
הוא מערך.** `.map()` על ריק אינו זורק — אין שגיאה, אין לוג, והבדיקות
האוטומטיות עברו כי בדקו שהטופס קיים ולא שיש בו מה למלא.

**את התקלה הכנסתי אני** בסקריפט `seed-showcase-cards.mjs`, שלא כתב את
השדות.

**מה נעשה:**
- `src/lib/contact-form.ts` — מקור אמת יחיד; ריק ולא-הוגדר מטופלים זהה
- מיגרציה `025` — מילוי **רק שורות ריקות**; דריסת בחירה מודעת של בעל
  העסק גרועה מהתקלה עצמה
- עורך שדות מלא: הצגה/הסתרה, placeholder, סדר
- חסימת פרסום של טופס בלי שדה למילוי — ממשק, שרת ומסד
- מזהה תקלה בכשל שמירת ליד; הטופס אינו מתאפס

### QA-006 · מלכודת הספאם · נסגר, אך לא כפי שדווח

**לא שוחזר:** השדה **אינו גלוי**. `sr-only` קיים ותקין ב-CSS של הייצור,
לצד `aria-hidden` ו-`tabindex="-1"`.

**מה כן נמצא, וחמור בהרבה:** השדות נקראו `website` ו-`company` — **שני
אסימוני autofill תקניים**. דפדפן עם מילוי אוטומטי מילא אותם, השרת ראה
מלכודת שהופעלה, והחזיר `{ok:true}` **בלי לשמור את הפנייה**. הלקוח ראה
"הפנייה נשלחה" והליד נעלם. בנוסף הסכמה הגדירה `max(0)`, ולכן מילוי
אוטומטי גרר גם שגיאת ולידציה גנרית.

זו כנראה הסיבה האמיתית גם ל-QA-033 — לא הגיעה התראה כי לא נוצר ליד.

### NEW-007 · מעבר בין כרטיסים · נסגר

**סיבת שורש:** `useState(initialCard)` מתעלם מה-prop אחרי ההרכבה
הראשונה. בהחלפת כרטיס העורך נשאר עם הנתונים הקודמים, והשמירה שלחה את
המזהה הישן — כלומר **עריכת כרטיס ב׳ נכתבה לכרטיס א׳**.
`key={card.id}` מאלץ הרכבה מחדש ומאפס גם את דגל ה-dirty ואת טיימר
השמירה האוטומטית.

### NEW-006 · כפילות Slug · נסגר

אינדקס ייחודי על `lower(slug)` (סוגר גם race condition), נתיב
`/api/cards/slug-check` עם חמישה מצבים והצעות מבוססות שם העסק, ונרמול
משותף לשני הצדדים — בדיקה שרצה על צורה אחרת מהשמירה חסרת ערך.

### QA-032 · נבדק, לא נמצא שבור

השמירה האוטומטית, `dirtyRef` ומצבי השמירה תקינים. הסיכון היחיד היה
זליגה בין כרטיסים, ונסגר ב-NEW-007.

---

## מה בוצע — קבוצה 2

### QA-009 · מיקוד שגיאות · נסגר

**שתי תקלות, שתיהן לא במקום שהדוח הצביע עליו:**
1. רכיבי `Field` בטופס הקשר לא העבירו `name`, ולכן `data-field` היה
   `undefined` וקישורי סיכום השגיאות חיפשו אלמנט שאינו קיים
2. טבעת המיקוד הוגדרה על `focus-visible` בלבד, ו-`focus()` תוכנתי אינו
   מפעיל את המצב הזה — המיקוד עבר, אבל שום דבר לא השתנה על המסך

### NEW-005 · OTP · נסגר, ועוד שתי תקלות בדרך

נוספו `pattern` ו-`enterKeyHint`, ו-`one-time-code` הועבר לכל התיבות —
iOS מציע את הקוד לשדה שבמיקוד, ומי שלחץ על התיבה השלישית לא קיבל דבר.

**נמצא תוך כדי:** החיצים היו הפוכים (המכל `dir="ltr"`), ו-`aria-labelledby`
הצביע למזהה שלא היה קיים.

### QA-033 · התראת ליד · נסגר

הסיבה השורשית תוקנה בקבוצה 1. כאן נוסף מעקב מסירה (מיגרציה `027`):
הליד נשמר תחילה, ורק אז נשלחת ההתראה; הסטטוס נרשם `sent` / `failed` /
`skipped`; סיבת הכשל נשמרת לאבחון **בלי תוכן הפנייה**.

### REQ-016 · מודאל בהרשמה · נסגר

הקישורים נווטו לעמוד נפרד ואיבדו את מה שהוקלד. עכשיו נפתחת החלונית
הנגישה ששימשה במסך עדכון התקנון.

---

## מה נשאר — קבוצה 3 · סנכרון הכרטיס

| מזהה | מה נדרש | מה ידוע |
| --- | --- | --- |
| **QA-024** | סנכרון פרטי קשר לפעולות | `contact-source.ts` קיים ונבדק. **דורש בדיקה חוזרת בעורך מחובר** |
| **QA-025** | נרמול WhatsApp מהעורך | `toE164` קיים ונבדק. נדרש: Preview של הקישור, וחסימת הפעולה כשהמספר פסול |
| **QA-026** | Instagram / Facebook | `socialUrl` קיים. נדרש: שגיאה גלויה כשהערך פסול, במקום הסתרה שקטה |
| **QA-027** | VCard | נדרשת בדיקה שה-VCF תואם ל**כרטיס הנבחר** אחרי NEW-007, ופתיחה בפועל ב-iOS/Android |
| **NEW-004** | תמונות הדוגמאות | **טרם נבדק.** נדרש מקור נתונים יחיד ל-Preview ולכרטיס החי, סט תמונות מאושר אחד, WebP/AVIF, srcset ובדיקת צילום משווה |

---

## מה נשאר — קבוצה 4 · שפה, SEO ותוכן

| מזהה | מה נדרש | מה ידוע |
| --- | --- | --- |
| **QA-035** | בורר שפה מלא | ⚠️ **מתנגש עם ה-HOLD על REQ-025** — ראו למעלה |
| **NEW-001** | "0 ימי ניסיון" ו-"0 רקעים" בדף הבית | **טרם נבדק.** המקורות קיימים (`TRIAL_DAYS`, `backgroundPresets.length`) — צריך לאתר את המופע שמציג 0 |
| **NEW-002** | שני H1 בדף הבית | **טרם נבדק.** `CardPreview` מקבל `compact` שמוריד ל-H2; צריך לוודא שדף הבית מעביר אותו |
| **NEW-003** | `NAIMLY \| NAIMLY` בכותרת | **טרם נבדק.** חשד: `title.template` ב-layout מוסיף סיומת לכותרת שכבר כוללת אותה |

---

## מה נשאר — קבוצה 5 · הרצה חוזרת של 17 הממצאים החסומים

QA-014, QA-015, QA-016, QA-017, QA-018, QA-019, QA-020, QA-021, QA-022,
QA-023, QA-024, QA-028, QA-029, QA-030, QA-031, QA-032, QA-034.

חלקם נבדקו בקוד ובמסד בסבבים קודמים, אך **הפרומפט דורש בדיקה חוזרת
בפועל בדשבורד מחובר**. שלושת חשבונות הבדיקה קיימים:

- `danbenyakov+1@gmail.com` — בסיסי
- `danbenyakov+2@gmail.com` — מקצועי
- `danbenyakov+3@gmail.com` — פרימיום

אין לסמן ממצא כ"תוקן" בלי בדיקה חוזרת בפועל וראיה.

---

## דרישות רוחביות שטרם טופלו

מהפרומפט, סעיפים 9–11:

- **סעיף 9 · אנליטיקס ופיקסלים** — אימות שהחלפת כרטיס טוענת רק את מזהי
  הכרטיס הנבחר, שאין טעינה כפולה, ושביטול הסכמה עוצר אירועים. רשימת
  האירועים הנדרשת: `card_view`, `contact_click`, `whatsapp_click`,
  `lead_submit`, `vcard_download`, `qr_download`, `file_download`,
  `card_share`.
- **סעיף 10 · בדיקות אוטומטיות** — רוב התרחישים מכוסים; חסרים בעיקר
  אלה שדורשים דפדפן (מעבר כרטיסים בפועל, Auto Save בזמן מעבר, תצוגה
  ב-5 רוחבים).
- **סעיף 11 · אבטחה** — RLS נבדק ל-17 תרחישים. נדרש אימות חוזר לאחר
  שינויי ריבוי הכרטיסים: שכל שאילתה מסוננת ל-`user_id` **ו**-`card_id`.

---

## פתוחים מסבבים קודמים

| # | מה | מי |
| --- | --- | --- |
| 1 | **סיבוב חמשת המפתחות שנחשפו בצ׳אט** — Supabase secret, סיסמת DB, סיסמת אפליקציה ב-Gmail, PAT, Vercel token. אומת שאינם בהיסטוריית Git, אך הם נחשפו בשיחה והריפו ציבורי | **בעל המערכת בלבד** |
| 2 | **REQ-025 מלא** — HOLD מ-15.09.2026 | בעל המערכת |
| 3 | **בדיקות ידניות בדפדפן** — כל מה שדורש משתמש מחובר | בעל המערכת / QA |
| 4 | **אימות תצוגת המיילים** ב-`info.naimly@gmail.com` — נשלחה דוגמה, לא אומת שהגיעה תקינה ב-RTL | בעל המערכת |

---

## עדכון דוח הביצוע

`NAIMLY_QA_FIX_REPORT.md` מעודכן עד קבוצה ו׳ של הסבב הקודם.
**טרם נוספו לו** ממצאי הרגרסיה הנוכחית: NEW-001 עד NEW-007 והממצאים
שנפתחו מחדש. יש להשלים אותו בסיום קבוצה 5, בפורמט שנדרש בסעיף 14
לפרומפט: מזהה, חומרה, סיבת שורש, קבצים, מיגרציה, תיקון, בדיקות,
תוצאות, ראיה, סטטוס וסיכונים.

---

## הצעד הבא

1. הכרעה בהתנגשות QA-035 / REQ-025.
2. קבוצה 3 — סנכרון הכרטיס, כולל NEW-004 שטרם נבדק כלל.
3. קבוצה 4 (בהתאם להכרעה).
4. קבוצה 5 — בדיקה חוזרת בדשבורד מחובר.
5. השלמת דוח הביצוע.
